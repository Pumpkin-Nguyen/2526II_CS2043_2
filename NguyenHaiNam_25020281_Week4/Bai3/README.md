# Class Employee có bắt buộc phải implement hàm work() của interface không?
- Không bắt buộc.
- Khi 1 abstract implement 1 interface, phương thức mẫu trong interface sẽ được truyền xuống cho lớp con của class abstract Employee. Vì vậy, có thể triển khai hàm work() hoặc không, tùy theo mục đích.