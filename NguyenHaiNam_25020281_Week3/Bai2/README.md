# Kết quả:

![alt text](image.png)

# Tại sao Duck lại kêu "Animal sound"?

- Vì trong lớp Duck, ta không khai báo đè hàm makeSound() nên Java sẽ tìm tới hàm makeSound() của hàm cha là Animal và gọi nó.  